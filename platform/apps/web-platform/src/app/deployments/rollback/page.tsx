export default function DeploymentRollbackPage() {
  return (
    <main>
      <h1>Rollback Center</h1>
      <p>Inspect failed deployments and trigger rollback workflows when needed.</p>
    </main>
  );
}
